const config = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'art.playukraine.com',
        pathname: '/**',
      },
    ],
  },
};

export default config;
